let releaseThumbnail: any = document.querySelectorAll(".releases-section .release-carousel .release-album-container #release-album-img img");

releaseThumbnail.array.forEach(element => {
    releaseThumbnail.addEventListener('click', () => {

    });
});